spring:
  datasource:
    url: jdbc:postgresql://localhost/department_management
    username: postgres
    password: midas256
  jpa:
    hibernate:
      ddl-auto: update
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
